#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - print all alphabeth except two
 *
 * Return: Always 0 for success
 */
int main(void)
{
	char ch = 'a';
	
	while (ch <= 'z' ch++)
		if (ch != 'q' && ch != 'e')
	{
		putchar(ch);
	}
	putchar('\n');
	return(0);
}
